# 交接文档：W1n 三模态机制实验 + 项目精简（2026-08-10）

> 复制本文件全文到下一个窗口。所有路径基于 `D:\hu\tongjiproj\20260727`。
> 上一份交接见 `HANDOFF_20260808.md`（副本 `20260810report/20260810report.md`）。

---

## 0. 本次会话做了什么

1. **项目精简**：创建 `20260810report/`（拷贝 8/8 交接文档为 `20260810report.md` + `回收站/`）；5 个与 affine 主线无关的旧项目（`ddpnm_2d_random_porous`、`fenicsx_real_porous_hoddpnm_berea_comparison`、`fenicsx_twophase_p2p1_hoddpnm_stokes_timesteps`、`stokes_audit_and_adaptive_fix`、`stokes_tracer_hoddpnm`）移入回收站，**未物理删除**。
2. **可复现性验证**：四核心脚本（`run_affine_ddpnm_benchmark.py`、`run_random_benchmark.py`、`run_ddpnm_3d.py`、`run_benchmark.py --skip-hoddpnm`）串行跑通，所有数值指标与 8/8 基线逐位一致（如 Affine L2 = 5.630% / 6.506% / 19.978%）。日志 `20260810report/run_check.log`。
3. **W1n 三模态实验（核心新工作）**：新增法向线性 3 模/面控制空间，在三个几何上完成机制分析——判断 Affine-9 的改善来自法向线性模还是切向线性模。结论：**法向线性模是普适主导项**（占绝对改善 59~83%），切向模贡献随几何不规则度上升（17%→41%→38%）。
4. **产出**：三几何正式输出（各项目 `outputs/benchmark_w1n/`）+ 四方法对比图 + README/RESULTS 文档更新 + 归档 `20260810report/W1n_experiment/`（含可运行代码快照，已从归档路径实际跑通验证）。
5. **git**：6 个新提交（见 §5），工作区干净。

---

## 1. W1n 实验（核心成果）

### 1.1 定义与构造

W1n = `span{1,s,t}⊗{n}`：每界面仅法向方向 × 标量 {1, s, t} 共 3 个广义模态
（界面中心为 s=t=0 的代表点）。`NormalLinearFaceBasis`（`affine_face_basis.py`）
仿 `AffineFaceBasis` 构造，覆盖 `active_indices`（全激活）/`global_keys`
（3 键，与 level 无关）。未知量链条：Classic 1/面 → W1n 3/面 → Affine 9/面。

### 1.2 三几何结果（速度相对 L2，同网格 vs 单片 FEM）

| 几何 | 界面数 | Classic | W1n-3 | Affine-9 | 法向模贡献 |
|---|---:|---:|---:|---:|---:|
| 均匀 27 球（12203 胞） | 144 | 21.08% | **8.16%** | 5.63% | 83% |
| 随机 27 球（15249 胞） | 114 | 65.32% | **30.75%** | 6.51% | 59% |
| 真实多孔 grid（32611 胞） | 144 | 92.54% | **47.38%** | 19.98% | 62% |

完整指标（broken-H1 / pL2 / 流量误差 / offline / online / 内存）：
- 均匀球：`affine_ddpnm_3d/outputs/benchmark_w1n/affine_ddpnm_metrics.csv`
- 随机球：`affine_ddpnm_3d_random_porous/outputs/benchmark_w1n/random_affine_metrics.csv`（含 Exact-FE-Schur 基线 vs 单片 1.05e-12）
- 真实几何：`real_porous_benchmark_3d/outputs/benchmark_w1n/benchmark_metrics.csv`（含内存：Classic 45 / W1n 79 / Affine 246 MiB）

### 1.3 机制结论

1. 经典 DDPNM 的瓶颈是「界面法向牵引只能取常数」（压力梯度面内投影无法表达），不是代表实体个数。
2. 均匀球上切向模只做收尾（流量 2.4%→0.32%），随机/真实几何下近半改善靠切向模（41% / 38%）。
3. W1n 用 Affine 1/3 未知量、≈Classic 离线时间、10~13× 在线加速消除 >2/3 绝对误差；要 0.3% 流量精度必须上 Affine 九模。

---

## 2. 代码改动清单（均已提交）

| 文件 | 改动 |
|---|---|
| `affine_ddpnm_3d/affine_face_basis.py` | +`NormalLinearFaceBasis`（W1n 基） |
| `affine_ddpnm_3d_random_porous/affine_face_basis.py` | 同步同一基 |
| `affine_ddpnm_3d/run_affine_ddpnm_benchmark.py` | 四方法（Classic/W1n/Affine/FEM），节编号 1/5..5/5 |
| `affine_ddpnm_3d/plot_affine_ddpnm_comparison.py` | 四系列图（W1n 紫 #7B1FA2，经调色板验证） |
| `affine_ddpnm_3d_random_porous/run_random_benchmark.py` | 五方法（+Exact-Schur），npz 增 `*_normal_linear` 场 |
| `real_porous_benchmark_3d/run_benchmark.py` | 四方法 + **内存修复**（见 §4.3） |
| `README.md`（顶层 §4.4）、`RESULTS.md` ×2 | 三几何 W1n 结果与机制结论 |
| `20260810report/` | 报告 md 追加 §6 + `W1n_experiment/` 归档 |

---

## 3. 环境与复现命令（重要）

```bash
# fenicsx 的 python 必须显式调用 + 导出 PATH，否则 LAPACK/原生库崩溃
export PATH="/d/Miniconda3/envs/fenicsx:/d/Miniconda3/envs/fenicsx/Library/bin:/d/Miniconda3/envs/fenicsx/Scripts:/d/Miniconda3/envs/fenicsx/Library/usr/bin:$PATH"
export PYTHONPATH=/d/hu/tongjiproj/20260727   # ddpnm_core / postprocess 依赖

# W1n 三几何 benchmark（输出各自 outputs/benchmark_w1n/）
cd affine_ddpnm_3d && /d/Miniconda3/envs/fenicsx/python.exe run_affine_ddpnm_benchmark.py --out-dir outputs/benchmark_w1n
cd affine_ddpnm_3d_random_porous && /d/Miniconda3/envs/fenicsx/python.exe run_random_benchmark.py --out-dir outputs/benchmark_w1n   # ~12 min，含精确 Schur 466s
cd real_porous_benchmark_3d && /d/Miniconda3/envs/fenicsx/python.exe run_benchmark.py --out-dir outputs/benchmark_w1n --skip-hoddpnm

# 验证运行（8/8 基线口径，输出 outputs/verify_run/）
cd ddpnm_3d_uniform_spheres && /d/Miniconda3/envs/fenicsx/python.exe run_ddpnm_3d.py --out-dir outputs/verify_run
```

绘图用 econ 环境：`/d/Miniconda3/envs/econ/python.exe`。

**性能实测**：均匀球 ~4 min；随机球 ~12 min（精确 Schur 峰值 commit ~6.9 GiB）；
真实几何 ~4 min（mesh 36s + FEM 15s + 三方法各 ~30s）。

---

## 4. 已知问题（按重要度）

### 4.1 【未修复】real_porous 的 slice 场保存 API 不匹配
`evaluate_fem_ddpnm_slice() got an unexpected keyword argument 'z_slice'`
（`run_benchmark.py` 调 `ddpnm3d/visualization.py`）。`slice_fields.npz` 不生成，
**不影响误差/成本表**。`plot_slice_errors.py` 可独立出切片图。

### 4.2 【已放弃】HODDPNM-adaptive 内存失败
完整管线在 `build_response_library(HierarchyBasis(...))` 时 commit 耗尽
（用户决定不再关注；用 `--skip-hoddpnm`）。

### 4.3 【已修复】real_porous 三库累积导致 Affine 崩溃
现象：经典/W1n 库构建后未释放，累计驻留 + 本机 commit 挤压（另有一个
外部 python 进程 PID 15752 长期占用 ~5 GB 并持续计算），Affine 9 模态库
构建时 `np.column_stack` 分配 1.52 MiB 失败。修复：Classic/W1n 块后加
`del lib; gc.collect()`。若再遇 `MemoryError: bad allocation`：等几分钟
（commit 平复）重跑即可，方法块本身无 bug。

### 4.4 其他
- 机器上存在非本会话 python 进程（如 PID 15752，16:18 启动、CPU 持续满载、
  工作集 ~5 GB）——真实几何的 FE 任务建议在它空闲时段跑，或预留内存余量。
- 验证日志与 `verify_run*/` 可清理（`.gitignore` 已忽略 `*.log` 与
  `**/outputs/verify_run/`，但 `verify_run_w1n/` 已被提交，如需清理用
  `git rm -r` 后提交）。
- `20260810report/回收站/` 内的 5 个旧项目仍可恢复（git 历史也有全部内容）。

---

## 5. Git 状态（当前 main，6 个新提交）

```
7b3dd49 feat: promote W1n benchmark results to official outputs in random and real porous projects
f639178 docs: archive W1n experiment into 20260810report with runnable code snapshot
d1e04d5 feat: extend W1n normal-linear control-space experiment to random and real porous geometries
a23db0b feat: W1n normal-linear 3-mode control-space experiment
a5f24a1 chore: archive old experiments to 20260810report/回收站; untrack pyc/settings
1113d3f rewrite README with Affine-DDPNM method and results   ← 本次起点
```

---

## 6. 待办 / 下一步建议

1. （可选）随机球 / 真实几何的 W1n 对比图：`plot_random_errors.py` 与
   `plot_errors.py` 未适配 5/4 方法（方法名硬编码），需要时扩展。
2. （可选）修 §4.1 的 `z_slice` 参数不匹配，恢复 slice 场输出。
3. （可选）跨几何 W1n 结论是否写入论文（`deepseekoutput/` 的 tex 文档链）。
4. （可选）清理 `verify_run_w1n/`（重复产物，正式输出已在 `benchmark_w1n/`）。
