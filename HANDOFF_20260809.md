# 交接文档：论文数值实验章节初稿（2026-08-09）

> 复制本文件全文到下一个窗口。所有路径基于 `D:\hu\tongjiproj\20260727`。
> 上一份交接见 `HANDOFF_20260810.md`（副本 `20260810report/HANDOFF_20260810.md`）。
> 本文件同日追加：§7（项目复制进 20260810report + 2D bug 修复）、§8（数值文档
> 版本现状与差异结论 —— 下个会话主线是**修改/替换数值文档**，先读 §8）、§9（场图
> 初版 + 明天任务清单）、§10（FIELD_DATA_TODO 补图会话：8 张图产出 + 5 张缺数据；
> **图件现状与待办以 §10.9 为准**）。

---

## 0. 本次会话做了什么

1. **读了上一份交接**，按用户给出的 27 条数值实验方案（四命题证据链：
   A 实现正确 / B Classic 瓶颈在 interface representation / C ablation
   区分 modal information / D modal requirement 随 geometry 变化），
   **编写论文数值实验章节初稿**：LaTeX + PDF + Markdown 三件套。
2. **交付位置（两份相同，均已编译验证）**：
   - `deepseekoutput/20260810_numerical_section/`
   - `20260810report/numerical_section/`（完整副本，图/数据/脚本齐全）
3. **新计算数据**：`data/geometry_stats.json` + 脚本
   `scripts/geometry_stats.py`（从分区网格算几何描述量）。
4. **新生成 6 张图 + 复用 1 张归档图**：`scripts/make_figures.py`（econ env）。
5. **（下午追加）项目复制进 20260810report 且可独立运行**：2D 移出回收站、
   6 个关键文件夹复制、依赖引导、**修复 2D 装配键契约 bug**、四项目全部
   验证与基线逐位一致、git 提交 ×2（详见 §7）。
6. **（晚间追加）数值文档两版本对比**：`gptoutput/numerical_experiments_revised_final`
   是 8/8 版初稿的保守化修订（Real-100→Heterogeneous-100、断言全面弱化、
   删附录 B 等），数值未变（详见 §8）。
7. **git**：下午两个提交后工作区干净（75482aa、9efc8bc）。

---

## 1. 论文数值实验章节（核心交付）

### 1.1 文件

| 文件 | 说明 |
|---|---|
| `numerical_experiments.tex` | 独立可编译（pdflatex，0 overfull / 0 undefined） |
| `numerical_experiments.pdf` | 11 页，7 张图全部嵌入 |
| `numerical_experiments.md` | 与 tex 内容一致的 Markdown |
| `figures/` | fig1 几何示意 / fig2 模态预算图（核心）/ fig3 四指标 dot plot / fig4 100% stacked shares / fig5 Pareto / fig6 切片误差场 / figA1 归档对比图 |
| `data/geometry_stats.json` | 几何描述量 |
| `scripts/` | geometry_stats.py（fenicsx env）+ make_figures.py（econ env） |

### 1.2 结构（按方案 §26，章节号 6–10）

- §6 Methodology：Table 1 几何统计（含 CV(Vᵢ)/CV(Aᵧ)/CV(a/b)）、Table 2 四空间预算、双参考系统说明、误差指标定义、gauge 处理、计时协议
- §7 Verification：Exact-FE-Schur 代码验证（1.05e-12）+ Table 3 代数诊断；§7.3 明确 full C-DD ξ 未算
- §8 Controlled：Table 4 绝对精度、Fig 2 模态预算（核心图）、Fig 3 四指标、§8.4 equal-budget 标为 planned、Fig 4 切片场
- §9 Realistic：Table 5 成本（如实写 Real-100 单次总时长慢于 FEM，仅 online 相快 255×/72×/10×）、Fig 5 Pareto
- §10 Cross：Fig 6 shares（83.6/16.4、58.8/41.2、62.2/37.8）、Table 6、§10.2 Schur-energy 分解 planned、§10.3 limitations + 6 项待办
- 附录 A：数据来源；附录 B：归档对比图

### 1.3 关键事实边界（写作时严格遵守，勿越界）

- **只有三个几何**：Uniform-27（27 球 / 64 分区 / 144 界面 / 12203 胞 / 63955 DOF）、
  Random-27（27 球 / 27 Voronoi 孔隙 / 114 界面 / 15249 胞 / 75954 DOF）、
  Real-100（100 球 / 64 grid 分区 / 144 界面 / 32611 胞 / 166841 DOF）。
  **不存在 Real-50**。
- **只有三个空间算过**：W0n（Classic）、W1n（NormalLinear）、W1v（Affine）。
  **W0v（常数切向模）从未跑过** → equal-budget 对比整节只能写 planned。
- **full C-DD ξ（broken-pressure 参考）从未算** → Pythagorean/Schur-energy
  验证整节只能写 planned，**没有数字**。Exact-FE-Schur（random，21970 trace
  DOF，465.9s）只定位为 Schur 消除代码验证（vs 单片 1.05e-12）。
- **Real-100 诚实细节**：FEM 总时长 14.5s < 任何 reduced 方法 total
  （27.98/28.17/34.13s）；online-only 加速才成立。压力误差非单调
  （uniform 上 e_p(W1n)=0.0185 < e_p(W1v)=0.0217）已写明是理论允许的。

---

## 2. 新计算的几何描述量（geometry_stats.py 产出）

| 几何 | N_p | m | 单元 | vel DOF | pres DOF | 混合 DOF | 2m/N_p | CV(Vᵢ) | CV(Aᵧ) | CV(a/b) | 平均 a/b |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Uniform-27 | 64 | 144 | 12203 | 60936 | 3019 | 63955 | 4.5 | 0.321 | 0.203 | 0.158 | 1.39 |
| Random-27 | 27 | 114 | 15249 | 72471 | 3483 | 75954 | 8.44 | 0.432 | 0.783 | 1.479 | 2.48 |
| Real-100 | 64 | 144 | 32611 | 159186 | 7655 | 166841 | 4.5 | 0.057 | 0.116 | 0.078 | 1.11 |

- **反直觉事实**：Real-100 的 grid 分区几何描述量最小（最规则），
  不规则度最大的是 Random-27。论文里如实写了。
- **FEM DOF 拆分公式已验证**：P2 = 3×(顶点+边)，P1 = 顶点；与求解器
  报告完全一致（63955 / 75954）。
- uniform/random 直接从保存的 `.msh`（meshio 解析）计算；real 无保存
  mesh，用 benchmark 自己的 `build_partition_grid` 重建（~36s）。
- aspect ratio = 面片顶点 PCA 两最大特征值比值的平方根（≥1）。

---

## 3. 图表生成注意事项

- 调色板：W0n 蓝 `#2a78d6`、W1n 橙 `#eb6834`、W1v 青 `#1baf7a`（dataviz
  验证过的分类槽）；FEM 参考用中性灰空心点；误差场用蓝色 sequential。
  归档对比图（figA1）保持原样（W1n 紫 `#7B1FA2`），附录 B 里说明了。
- `make_figures.py` 从三个 `outputs/benchmark_w1n/*.csv` + random 的
  `report.json` + `npz` 读取，**不硬编码数值**（除 FEM online 时间三个
  常数，已在脚本里注明来源）。
- 误差场图只用 Random-27 的 npz（uniform/real 无切片数据；real 因
  z_slice bug 跳过）。

---

## 4. 已知问题（按重要度）

### 4.1 【未修复】W1n 归档入口无法直接运行
`20260810report/W1n_experiment/code/run_{affine_ddpnm_benchmark,
random_benchmark,benchmark}.py` 开头硬编码检查
`REPOSITORY_DIR / "ddpnm_3d_uniform_spheres"`（REPOSITORY_DIR =
`W1n_experiment/`），归档未携带该 base project（含 `ddpnm3d` 包）。
现象：`FileNotFoundError: Expected the unchanged base project at
...\W1n_experiment\ddpnm_3d_uniform_spheres`。
修复选项（任选）：
- 建目录 junction：`cmd //c mklink /J
  W1n_experiment\ddpnm_3d_uniform_spheres D:\hu\tongjiproj\20260727\ddpnm_3d_uniform_spheres`
- 或运行时 `PYTHONPATH` 加仓库根的 `ddpnm_3d_uniform_spheres/`（`affine_face_basis.py`
  已可导入，只差这层检查）。

### 4.2 【未修复】回收站旧项目不能独立运行
`20260810report/回收站/` 的 5 个旧项目代码在，但依赖仓库根
`ddpnm_core`/`postprocess` 及原始数据，单拿出去跑不起来。
用户 8/9 问过「之前所有实验的代码能不能进入」，回答：仓库根原位可跑，
20260810report 里不能独立入口。**用户说「明天再说吧」——这是明天的
待议事项。**

### 4.3 延续自上一份交接
- real_porous 的 `z_slice` 参数不匹配（§4.1 旧档），slice 场不生成。
- HODDPNM-adaptive 内存失败已放弃（`--skip-hoddpnm`）。
- 机器上有非本会话 python 进程占内存（旧档 PID 15752），FE 任务预留余量。

---

## 5. Git 状态

- main 分支；本次新增两个未跟踪目录：
  `deepseekoutput/20260810_numerical_section/`、
  `20260810report/numerical_section/`（**未提交**，工作区其他干净）。
- 上一份交接的 6 个提交（a23db0b…7b3dd49）不受影响。

---

## 6. 待办 / 下一步建议

0. **（下个会话主线，用户已定）数值文档修改与替换**：以
   `deepseekoutput/gptoutput/numerical_experiments_revised_final/`（B 版）为
   工作对象修改，替换旧版（A：`deepseekoutput/20260810_numerical_section/`；
   A'：`20260810report/numerical_section/`）。先读 §8 的差异清单与替换注意
   （B 缺 .md、figures/ 完整性、替换目标范围需用户确认）。
1. **（旧项已部分解决）旧实验入口问题**：回收站 5 项目中 2D 已移出并修复；
   剩 4 个（fenicsx_real_porous_hoddpnm_berea_comparison、twophase、
   stokes_audit、stokes_tracer）仍在回收站，未打包，保持「仓库根原位可跑」。
2. **（可选）修 W1n 归档入口（§4.1）**：现在 `20260810report/` 根下已有
   `ddpnm_3d_uniform_spheres`，把 `W1n_experiment/code/run_*.py` 的兄弟目录
   检查改为查 `W1n_experiment/` 上一级即可修好（junction 或改检查路径）。
3. **（论文补全，需新跑实验）**：
   - W0v（常数切向模）三几何各跑一遍 —— equal-budget 对比的关键，
     代码只差一个 basis（仿 `NormalLinearFaceBasis` 构造，键为
     3 个方向常量），每个几何 ~4–12 min；B 版把该节标为 planned；
   - full C-DD ξ（Uniform-27 + Random-27）—— Schur-energy/Pythagorean
     验证，需要新的求解器代码（broken-pressure 全迹系统）；
   - mesh sensitivity（Uniform-27 三档网格）；
   - Random-27 多 realization 统计（5 个 seed）；
   - （B 版新增 (g)）统一计时口径的最终性能基准。
4. **（已完成）** 今日交付已提交（75482aa + 9efc8bc）。
5. **（可选）** 图 2 的 r=3 位置等 W0v 跑完后加第二个点，形成
   「方向 vs 空间」同预算对比；Fig 4（shares）的 caption 已写明是
   descriptive 非因果 attribution，勿改。

---

## 7. （追加，同日）项目复制进 20260810report 且可独立运行

用户指示：2D DDPNM 从回收站拿出来；20260727 里最关键的文件夹
（3D 三几何 + 共享依赖）复制进 `20260810report/`；保证在新位置可运行、
输出与基线一致。**全部完成并验证**。

### 7.1 目录布局（复制，原件未动）

```
20260810report/
├── ddpnm_2d_random_porous/        ← 从 回收站/ 移出（原在 20260810report 内）
├── ddpnm_core/                    ← 复制（re-export shim，imports postprocess）
├── postprocess/                   ← 复制（ddpnm_core 的 metric/field 实现）
├── ddpnm_3d_uniform_spheres/      ← 复制（165M，含 ddpnm3d 包 + outputs）
├── affine_ddpnm_3d/               ← 复制
├── affine_ddpnm_3d_random_porous/ ← 复制
├── real_porous_benchmark_3d/      ← 复制
└── （原有：W1n_experiment/、numerical_section/、回收站/ 剩4个、HANDOFF）
```

20260727 根下原件全部原位未动；`ddpnm_core`/`postprocess` 字节级一致。

### 7.2 依赖处理

- 3D 各 run 脚本原本就按 `PROJECT_DIR.parent`（=20260810report）推导仓库根，
  依赖同级兄弟目录（`ddpnm_3d_uniform_spheres` 提供 `ddpnm3d` 包等）→
  复制到同级后**零改动**可跑。
- 2D 的 4 个入口（`run_ddpnm_2d.py`/`run_adaptive_ddpnm_2d.py`/
  `run_strict_error_2d.py`/`audit_mesh.py`）原本依赖外部 PYTHONPATH，已加与
  3D 相同的 sys.path bootstrap（`PROJECT_DIR.parent`），**不再需要 PYTHONPATH**。
- 全程验证均在无 PYTHONPATH 环境下从 `20260810report/` 内直接运行。

### 7.3 【修复了一个真 bug】2D 装配键契约不匹配

现象：2D 跑出全零结果（velocity 误差 1588% vs 基线 4.5%，Schur min eig=0）。
根因：`ddpnm2d/basis_2d.py::PolynomialNormalBasis.global_keys` 返回 2 元组
`(interface_id, degree)`，而统一装配器 `ddpnm_core/assembler.py` 匹配 3 元组
`(interface_id, component, polynomial)`（3D 各基的契约）→ 键永不匹配 →
Schur 全零。这是 ddpnm_core 统一装配重构后 2D 未适配的回归（2D 后来一直
没再验证过）。修复：`basis_2d.py` global_keys 改 3 元组 +
`solver.py` 两处 key 查找。`HierarchyBasis` 不动（自适应路径自装配自有键）。
修复记录已写进 `ddpnm_2d_random_porous/README.md`。

### 7.4 验证结果（全部通过，无 PYTHONPATH）

| 项目 | 运行路径（复制件） | 结果 |
|---|---|---|
| 2D | `20260810report/ddpnm_2d_random_porous` | 8 项检查 PASS；velocity=4.455%/pressure=0.951% 与基线一致 |
| 3D 规则 | `20260810report/affine_ddpnm_3d` | csv 与基线 **diff=0.00e+00**（21.08/8.16/5.63%） |
| 3D 随机 | `20260810report/affine_ddpnm_3d_random_porous` | csv 与基线 **diff=0.00e+00**（65.32/30.75/6.51%，Exact-Schur 1.05e-12） |
| 3D 真实 | `20260810report/real_porous_benchmark_3d` | csv 与基线 **diff=0.00e+00**（92.54/47.38/19.98%） |

临时验证输出（`outputs/verify_w1n/`、`_verify_baseline/`、git worktree）已清理。

### 7.5 目录搬移兼容性（用户咨询过）

- **改 20260727 名字 / 把 20260810report 移到 tongjiproj 根下均可行**：
  全部代码基于 `Path(__file__).parent` 相对推导，无 `20260727` 硬编码
  （唯一例外：根级 `plot_classic_vs_affine.py:180` 硬编码输出目录；
  回收站 2 脚本引用 `D:\hu\tongjiproj\FENICSX\` 外部目录）。
- 移动时注意：git 仓库边界（20260810report 移出后脱离 20260727 仓库，
  git 跟踪过的回收站文件会显示删除）；HANDOFF/README 路径文字会过时；
  W1n 归档 §4.1 问题（`W1n_experiment/code/run_*.py` 找不到兄弟目录）仍在，
  但现在 `20260810report/` 根下已有 `ddpnm_3d_uniform_spheres`，顺手可修。

### 7.6 Git 状态（已提交）

- 两个提交，工作区干净：
  - `75482aa` docs: numerical experiments section draft (tex/pdf/md, incl. revised final)
  - `9efc8bc` feat: copy runnable projects into 20260810report; fix 2D basis key contract
- 前者顺带提交了 8/9 早先未提交的论文交付（`deepseekoutput/20260810_numerical_section/`、
  `deepseekoutput/gptoutput/`、`20260810report/numerical_section/`）。

---

## 8. 数值文档版本现状（下个会话主线：修改/替换）

用户下一步要对数值文档做**修改和替换**。现有三个版本：

| 版本 | 路径 | 内容 | 修改时间 |
|---|---|---|---|
| A（旧初稿） | `deepseekoutput/20260810_numerical_section/numerical_experiments.tex/.pdf/.md` | tex 40330B，三件套齐全 | 8/8 23:47 |
| A'（A 的副本） | `20260810report/numerical_section/`（含 figures/ data/ scripts/） | 与 A 相同 | 8/8 23:47 |
| B（修订版） | `deepseekoutput/gptoutput/numerical_experiments_revised_final/`（`numerical_experiments_revised.tex/.pdf` + figures/） | tex 40369B，**无 .md** | 8/9 01:45 |

A 与 B 的关系：B 是 A 的**保守化修订**，66 处变更块/454 行差异，**数值逐位未变**。
B 的标题页改为 "Standalone working version"，`\setcounter{section}{5}` 对齐全文 6–10 章。

### 8.1 B 相对 A 的实质差异（修改时的决策依据）

1. **命名**：Real-100 → **Heterogeneous-100**（§9 标题改「Larger heterogeneous
   configuration」；定义加 "synthetic" + "project-internal label 'Real-100'"；
   表 1/3/4/5/6、图注、正文全部同步）。去掉 "realistic" 的隐含真实性断言。
2. **断言弱化（去因果/去外推）**：
   - 加速比：A 如实报「Real-100 单次 reduced 比 FEM 慢、online 快 255×/72×/10×」；
     B 称归档 FEM 时间是 solve-only、**不支持 like-for-like 首解加速比声明**，
     待办新增 (g)：统一计时口径重跑。
   - Exact-FE-Schur：B 只定位为「验证全迹 Schur 消除与误差评估管线，
     **不验证** affine 模态基装配」。
   - Schur 最小特征值：B 明示 λmax 未归档、**不算 condition number 估计**。
   - §8.3 要点：A「误差随几何偏离规则晶格升至 65–93%」→ B「observed
     model-to-FEM discrepancies；无 C-DD 参考时不认定为纯模态缩减误差」。
   - §10.1 shares：B 指出 Heterogeneous-100 的 CV 比 Uniform-27 还小，
     **不支持「不规则度→模态份额」标量律**（A 只说不构成因果）。
   - 内存：B 不推断复杂度律；时间协议承认 CPU/RAM/软件版本元数据未记录。
3. **表格/图**：
   - 表 4 删掉 Random-27 专属的 mean-aligned 压力列（6 列→5 列）。
   - 图 5 Pareto：FEM 自误差为 0 无法上对数轴 → 改为**垂直参考线**，
     每配置分面板（图宽 0.62→0.97）；A 画了 FEM 空心点。
   - 图 6 切片场：B 明确**定性**（归档图未保留 colorbar 数值）。
   - 图 4 caption 点名中间两空间 $\Wspace_{0v}$/$\Wspace_{1n}$ 非嵌套。
   - 表 1/表 5 加 `\resizebox{\textwidth}{!}`。
4. **附录**：B **删除附录 B**（归档对比图 A1）；附录 A 简化为一句
   （数值保留自工作文档、图从表值重生成、未跑实验不给数值）。
5. **引用**：硬编号 `\S7.3/\S8.4/\S10.2/\S10.3` → `\S\ref{...}` 交叉引用。
6. **待办清单**：(a)–(f) → (a)–(g)；新增 (g) 统一计时基准；z_slice 原因不再明说。

### 8.2 替换时注意

- **B 缺 .md**：A 有 `numerical_experiments.md`（与 tex 内容一致）。若替换到
  A'/A 位置，需决定：保留旧 .md、生成新版 .md，还是 md 不再维护。
- B 的 figures/ 是否已含全部新图（fig5 分面板版本等）？替换前核对
  `figures/` 与 tex 的 `\includegraphics` 引用是否一一对应。
- 替换目标未定：只换 tex？还是连同 pdf/md/figures？B 是源（唯一新版），
  A'（20260810report/numerical_section/）是已提交的副本。
- B 已提交进 git（75482aa）。

---

## 9. （追加，同日深夜）云图升级：fig6 2×4 已交付，fig7 数据明天跑

用户指示：云图从「展示型」升级为「证据型」——坐标轴、colorbar、误差 panel 行内共用
色标、panel title (a)-(h)、球体/接口轮廓、新增代表性界面局部图 q=u·n。**今天只把
现成的数据写进文档，缺的数据列清单明天跑。**

### 9.1 数据盘点结论（关键事实）

| 几何 | 4 方法切片（fig6） | 接口局部 q=u·n（fig7） |
|---|---|---|
| Random-27 | ✅ 旧 npz 就有 | ❌ 缺（需重跑导出） |
| Uniform-27 | ✅ **今天补跑产出** | ✅ **今天补跑产出** |
| Real-100 | ❌ 论文口径无场（z_slice bug 未修） | ❌ |

- Uniform 重跑 ~3 min（mesh+FEM 77s 主导），误差值与基线**逐位一致**
  （21.084/8.162/5.630%）；timing 有噪声，新 csv/report 已移入
  `回收站/20260809_uniform_benchmark_w1n_timings/`，基线已从备份恢复。
- random 重跑**今天跑了一半被叫停**（用户：只写现成的）——目录完好无损
  （csv/report/npz/msh 全部与基线字节一致，已验证）。

### 9.2 代码改动（已就绪，未全部跑）

- `ddpnm_3d_uniform_spheres/ddpnm3d/visualization.py` 新增：
  - `evaluate_fem_ddpnm_interface`——接口 facet 上求 FEM/DDPNM 的 u·n，附归一化
    面内坐标 (s,t)∈[-1,1]²（按最大面内半径缩放），顶点取首个引用 facet 的父胞
    （**踩过的坑**：去重顶点数 ≠ facet 数，dolfinx eval 报
    "Number of points and number of cells must be equal"，已按顶点记父胞修复）
  - `fem_interface_fluxes`——FEM 法向通量 per 接口，用于自动选代表接口
    （|flux| 最大者；uniform 选出 id=56/144）
  - `interface_contour_segments`——切片平面上的接口轮廓线段
  - `_interface_facets`——facet→parent cell 公共 helper
- `affine_ddpnm_3d/run_affine_ddpnm_benchmark.py`：去掉两个 `del`（保留三解），
  末尾加切片+接口导出 → `affine_benchmark_fields.npz`（**已跑通交付**）
- `affine_ddpnm_3d_random_porous/run_random_benchmark.py`：`_write_outputs` 加接口
  导出（**未跑，等明天**）
- `numerical_section/scripts/make_figures.py`：
  - REPO 路径改为自动探测（parents[2]/parents[3]，报告副本与 deepseekoutput 副本
    都能跑）
  - `fig6_fields` → **2×4**（行=Uniform/Random，列=FEM |u| + 三误差）；参考 panel
    橙色 ramp（palette slot 2 家族）、误差 panel 蓝色 ramp（palette steps
    100→700）、行内共用一根误差 colorbar、每行参考 panel 独立 colorbar、坐标轴
    物理坐标、球体截面轮廓（灰圆）、接口轮廓线（灰线，仅 uniform 行有——random
    行缺 interface_slice_segments，明天补）
  - `fig7_interfaces`：2×4 q=u·n，blue↔red 发散 + 中性灰中点，行内 4 panel 共用
    色标；**数据不全自动 skip 并打印提示**（今天 skip）
  - 色标 hex 已过 dataviz 验证器（categorical 检查不适用连续 ramp，仅要求
    lightness 单调，通过）

### 9.3 文档交付状态（B/A/A' 三副本）

| 副本 | tex | md | PDF |
|---|---|---|---|
| B（工作对象）`deepseekoutput/gptoutput/numerical_experiments_revised_final/` | ✅ §8.5 重写 | B 本无 md（§8.2 待决） | ✅ 11 页 02:30 编译，0/0 |
| A `deepseekoutput/20260810_numerical_section/` | ✅ | ✅ 同步（§8.5 + 附录） | ✅ 02:30 编译，0/0 |
| A' `20260810report/numerical_section/` | ✅ | ✅ 同步 | ⚠️ **旧 PDF 被进程锁定**，`numerical_experiments_v2.pdf` 已编译好（12 页 0/0），没换名 |

- 新 §8.5 内容：2×4 布局描述、行内共用色标声明、物理坐标、切片 z=0.5、
  球体/接口轮廓说明、定量叙述（替换了旧「无 colorbar 只能定性」的免责声明）、
  结尾注明接口通量场与 Heterogeneous-100 场输出为 planned（§10.3 (e) 已扩充）。
- 附录 A 溯源补充 `affine_benchmark_fields.npz` + fig:fields 重生成来源。
- **A' PDF 锁**：`numerical_experiments.pdf`（8/8 旧版）被未知进程句柄锁定
  （mv/cp 均 WinError 32；非 15752 遗留进程、非 20740、无阅读器进程，疑似
  Explorer 预览/杀毒瞬态锁）。处理：关掉打开它的程序后
  `mv numerical_experiments_v2.pdf numerical_experiments.pdf`，或直接删锁。
- **md 决策已定**：用户说「改一下 md 好了」→ md 保留并同步（A/A' 已做）；
  B 的 md 是否生成仍待决。

### 9.4 明天任务（详见 `20260810report/FIELD_DATA_TODO_20260809.md`）

1. **Random-27 接口导出重跑**（~3-5 min，代码已就绪）→ fig7 2×4 完整 + fig6
   random 行补接口轮廓线。命令见清单文件（fenicsx env + PATH 导出）。
2. **Real-100 场数据**（先修 z_slice bug，~5 min）→ 可选第三行。
3. 跑完把新 csv/report 移回收站、基线恢复（同 uniform 处理）。

### 9.5 用户规矩与决策（本会话新增）

- **删除进回收站**：任何文件删除/覆盖不物理删除，移入 `20260810report/回收站/`
  （已存 memory：no-physical-delete-recycle-bin）。
- 今天只写现成数据，缺的列清单明天跑。
- `.claude/settings.local.json` 的 `defaultMode: "dontAsk"` 被用户撤销（保持默认询问）。

### 9.6 关键科学注意（fig7 设计决策依据）

**q=u·n 在 W1n 和 W1v 下都是 (s,t) 的线性函数**（切向模式在自身界面上法向分量为
零；W1v 对 q 的改进只能通过相邻孔隙的网络耦合体现）→ fig7 的 W1n/W1v 对比会较弱。
若需要直接展示 affine 切向结构，**traction τ_n=(σ·n)·n** 是更强的量（需评估应力，
代码未写）。fig7 caption 措辞已留余地（「FEM reference and the three reduced spaces」，
未声称 affine 在 q 上的直接结构增益）。

### 9.7 git 状态（今天未提交）

未跟踪/已修改（等待用户决定是否提交）：
- 修改：`ddpnm3d/visualization.py`、`affine_ddpnm_3d/run_affine_ddpnm_benchmark.py`、
  `affine_ddpnm_3d_random_porous/run_random_benchmark.py`、`make_figures.py`、
  B/A/A' 的 tex+md、`HANDOFF_20260809.md`（本文件）
- 新增：`affine_benchmark_fields.npz`、`FIELD_DATA_TODO_20260809.md`、
  `回收站/20260809_uniform_benchmark_w1n_timings/`、A' `numerical_experiments_v2.pdf`
- 备份目录 `_backup_uniform_benchmark_w1n/`、`_backup_random_benchmark_w1n/`
  （验证完可清，但按 9.5 规矩移回收站而非物理删）
- 8/9 早先未提交项（HANDOFF_20260809.md §5 所述）不受影响

---

## 10. 同日续：FIELD_DATA_TODO 补图会话（图集产出，2026-08-09 晚）

> 本会话按 `20260810report/FIELD_DATA_TODO_20260809.md`（规格全文，即上一份交接
> §9.4 的「明天任务」清单）执行。**所有输出在 `deepseekoutput/gptoutput/`，
> 未进入 20260810/ 或 20260810report/。**

### 10.0 本次会话做了什么

1. **数据盘点**（§10.2 结论）：三几何现有数据能生成 8 张图；5 张缺数据。
2. **风格学习**：读了 `affine_ddpnm_3d_random_porous/plot_random_errors.py`
   （`outputs/benchmark/01_slice_error_fields.png` 的生成脚本），复用其场渲染
   机制（子域内 LinearNDInterpolator + 掩膜高斯平滑、球截面、子域边界线、
   sci 色标、serif 面板字母）+ 数值章节已验证色板（蓝/橙/青，W0v 预留紫
   #4a3aa7）。
3. **新脚本**：`deepseekoutput/gptoutput/scripts/figlib.py`（数据层+样式）
   与 `make_final_figures.py`（13 个图函数，数据缺失自动守卫跳过并打印原因，
   补数据后重跑即出）。
4. **产出 8 张图**到 `deepseekoutput/gptoutput/figures/` + 说明
   `figure_notes.md`（每图 caption、数据来源、缺失清单、LaTeX caption）。
5. **补数据**：uniform 场 npz（`affine_benchmark_fields.npz`）从
   `20260810report/affine_ddpnm_3d/...` 归档**拷回主项目**
   `affine_ddpnm_3d/outputs/benchmark_w1n/`（此前主项目缺这份场数据，
   只有归档副本有）。
6. **未提交任何 git**（含 §9.7 未提交项，等用户决定）。

### 10.1 产出图（8 张，`deepseekoutput/gptoutput/figures/`）

| 图 | 内容要点 |
|---|---|
| fig2_modal_budget_fourspace.png | r=1,3,9 vs 速度 L2，log-y，三面板同 y 范围；只连嵌套路径 W0n→W1n→W1v；W0v 数据出现后自动加 r=3 紫点 + 虚线路径 W0n→W0v→W1v |
| fig3_metrics_fourspace.png | 速度 L2 / broken-H1 / 压力 L2 / 流量误差四面板，log-y；只沿嵌套链画虚线，W0v↔W1n 永不连线 |
| fig5_fields_uniform27.png | z=0.5 切片：FEM \|u\|（独立 viridis 色标）+ W0n/W1n/W1v 误差（**共享** turbo 色标，98 分位池化），球截面+界面迹线 |
| fig5_fields_random27.png | 同上布局，random 几何 |
| fig5_interface_flux_modes.png | 代表界面 q(s,t)=u·n，FEM/W0n/W1n/W1v，行内共用发散色标；**仅 uniform 行**（random 无导出数据） |
| fig7_accuracy_cost_standardized.png | 每几何一面板：实心=online、空心=首次求解（含 offline）、虚线连接；FEM 为竖直参考线标注 error=0 不在对数轴；单次计时无散布 |
| fig10_observed_shares_clean.png | W0n→W1n→W1v 缩减份额 100% 水平堆叠，图例在绘图区外；caption 已注明是路径依赖观测份额非因果贡献 |
| fig1b_geometry_partition_interfaces.png | 2×3：上排固体几何、下排实际分区/界面网络；uniform/real 画笛卡尔网格界面线（0.2/0.5/0.8 与 0.25/0.5/0.75，各 144 界面），random 画真实四面体分区（按子域着色）+ 喉道连线 + 界面中心点；视角一致 |

### 10.2 关键数据事实（写图依据，重要）

- **W0v（常数向量 span{1}⊗{n,t1,t2}，3 模/面）在任何运行中都不存在**。
  三几何 benchmark 只有 W0n(1)/W1n(3)/W1v(9)。→ fig4 无法生成（守卫跳过）；
  fig2/fig3 为三方法版。**fig4 函数已写好**，只要 CSV 里出现 W0v 行
  （`_METHOD_MAP` 加映射即可）自动激活。
- **Real-100 当前 grid 分区 W1n benchmark 无切片场**（`z_slice` API bug，
  HANDOFF_20260810 §4.1）。旧 `outputs/20260806/slice_fields.npz` /
  `benchmark_voronoi_100/` 是 Voronoi-100 分区（537 界面）的 classic/affine
  场，**规格明确禁止用于定量** → fig5_fields_real100 跳过。
- **Random-27 npz 无 interface_q 键**。导出代码只存在于
  `20260810report/affine_ddpnm_3d_random_porous/run_random_benchmark.py`
  （~573-612 行：representative_interface_id / interface_q_fem / classic /
  normal_linear / affine）——即 §9.4「代码已就绪」所指；**主项目
  run_random_benchmark.py 没有这段代码**。uniform 的 interface_q 数据也是
  归档副本脚本跑出来的。
- uniform 场 npz 无 sphere/cell_labels 键 → 脚本用坐标推导笛卡尔子域标签
  （CELL_EDGES=0/0.2/0.5/0.8/1，4×4×4=64 胞）；random npz 有完整键。

### 10.3 复现命令

```bash
/d/Miniconda3/envs/econ/python.exe deepseekoutput/gptoutput/scripts/make_final_figures.py
```

输出 `deepseekoutput/gptoutput/figures/`，状态摘要打印 [OK]/[SKIP]+原因。
说明文档 `deepseekoutput/gptoutput/figure_notes.md`。

### 10.4 下一会话待办（按优先级）

1. **用户决策：是否补 W0v**。扩展三项目 run 脚本加 VectorConstant 基并重跑
   （均匀 ~4min + 随机 ~12min + 真实 ~4min，fenicsx env + PATH 导出）→
   fig4 + fig2/fig3 完整四空间版 + fig5 各加 W0v 面板。本会话因用户指示
   「先做已有数据的图」未做，**未征询用户**（当时 AskUserQuestion 被权限
   模式拒绝）。
2. **Random-27 接口导出**：把归档副本导出代码 port 回主项目
   `run_random_benchmark.py` 并重跑（~12 min）→ fig5_interface_flux 补
   random 行 + fig1b random 面板补界面迹线。跑完按 §9.5 规矩把新产物
   移回收站、基线恢复（同 uniform 处理）。
3. **Real-100 场**：修 `evaluate_fem_ddpnm_slice()` 的 `z_slice` 参数不匹配
   （`run_benchmark.py` ↔ `ddpnm3d/visualization.py`，一行级修复）→ 重跑
   （~4 min）→ fig5_fields_real100。
4. **fig6/fig8/fig9** 需新实验数据（broken-pressure C-DD 能量分解 / ≥3 套
   网格的 mesh sensitivity / 多种子 random 集成），现有脚本函数已就绪。
5. **git**：本会话新增（gptoutput 脚本+图+notes、uniform npz 补回）+ §9.7
   未提交项，等用户决定后提交。

### 10.5 本会话环境备注

- 会话期间权限为 dontAsk 模式，Write/Bash 写文件被自动拒绝多次；用户放行
  （切换权限模式）后恢复。**遇到权限拒绝先向用户说明，不要反复重试**。
- 无代码改动（除新增 gptoutput 脚本外，solver/plot 脚本均未动）。

### 10.6 评审修改轮（8 张图逐条修订，2026-08-09 夜）

用户评审后逐条修复，已全部重生成（`deepseekoutput/gptoutput/figures/`）：

| 图 | 评审意见 | 修复 |
|---|---|---|
| fig2 | 不能叫 four-space，等 W0v | 注释注明「W0v at r=3 pending」；W0v 数据出现后自动画虚线路径 |
| fig3 | outlet flux 纵轴刻度格式不统一 | 四面板统一 3 位有效数字百分比刻度 |
| fig5_fields×2 | 面板/colorbar 挤压、W0n 几乎消失、坐标标签重叠 | 布局加宽（14.5×4.15、wspace 0.42、色标列 0.09）；**共享色标上界从池化 p98 改为 p90(W0n)**（p98 下 W0n 中位数只占 ~8% 色标，p90 下 ~30%，W1n/W1v 残差结构仍可见）；标题改短；刻度字号 7.5 |
| fig5_interface_flux | 补 (s,t) 数值刻度 + flux-error row | s/t 五等分刻度；新增第二行 |q−q_FEM| 误差图（共享 p90 色标） |
| fig7 | 前提：三几何 FEM timing scope 统一 | 无法统一数据，图内加注记声明口径差异（uniform/random=solve only，real=total_s），notes 标「最终使用前需统一计时协议」 |
| fig10 | legend 与 x-label 重叠 | 图例改两列并下移（bbox -0.40），subplots_adjust bottom 0.30；**bbox 级验证 overlap=False** |
| fig1b | Random-27 下半行渲染风格与另两个完全不同 | 下半行统一为**界面线框**：random 改为从真实四面体网格提取界面三角形边（两子域共享面），去掉子域着色/喉道线/中心点；行标题改「Finite-area internal interfaces」 |

注：fig10 的 PNG 在本会话查看器中无法渲染（其他图正常，格式/尺寸无异常），
改用 matplotlib bbox 程序化验证图例/标签无重叠。重跑命令不变（§10.3）。

### 10.7 场图布局 bug 修复（2026-08-09 深夜）

用户指出 fig5_fields×2「图和文字重合、无法入论文」。定位：
`fig5_fields` 里 `axes = [fig.add_subplot(gs[0, c]) for c in range(4)]` 把 W0n
面板放进了**列 1**（恰好是 ref 色标的槽位，width_ratios 0.09），两轴重叠（bbox
诊断：面板与色标同处 x=439-457px）。修复：两色标改放右侧两列
（width_ratios [1,1,1,1,0.055,0.055]），面板占 0-3 列。
**全量 bbox 审计**（monkeypatch savefig 捕获 8 张图，检查轴-轴/文字-轴
重叠）：TOTAL PROBLEMS = 0。后续改任何布局都先跑这个审计再交付。
另：根目录版 `deepseekoutput/{figlib,make_final_figures}.py` 是另一模型对
本会话脚本的修订（矢量误差 `‖u_DD−u_FEM‖` 修正 + fig2 抖动 + 动态图例 +
FEM_TIMING_SCOPE 常量等），**尚未跑过**（无 deepseekoutput/figures/），
也未合并进 gptoutput 版；其 FEM_TIMING_SCOPE 的 Real-100="solve only"
经查证为误（三几何 FEM 计时都是 solve_reference 全程=装配+求解）。

### 10.8 第二轮评审修复：色标数字重叠 + uniform 白边（2026-08-09 深夜）

用户评审：① 两张误差云图右侧双色标的刻度数字互相重合；② uniform（规则几何）
误差云图界面处仍为白色边界。已修复（gptoutput/scripts/ 版）：

1. **色标间距**：`fig5_fields` 的 gridspec 改 7 列
   `[1,1,1,1,0.06,0.14,0.06]`，两色标间加空列 0.14，刻度不再相碰。
2. **uniform 白边根因**：uniform 的切片三角化**不贴合网格分区**——364/2280
   个三角形跨越分区平面、无任何共享边连接不同子域（逐三角形标签的边界检测
   得 0 段），逐子域插值的凸包收缩 → 界面处全 NaN（白带）。修复：
   `slice_field_grids` 增加 `global_interp=True` 路径（全顶点插值，NaN 仅存
   于球洞 31%）+ `boundary_lines` 覆盖；uniform 改画**解析迹线**
   （x/y=0.2/0.5/0.8，球洞裁切，24 段，`uniform_interface_trace_lines`）。
   random 改为用归档的真实 parent-cell 逐三角形标签（保持贴合三角化的黑迹线）。
3. **审计**：bbox 全量审计 0 问题；uniform NaN 只剩球洞；两图目检通过。

注意：uniform 的切片三角化不贴合分区这一点同样影响
`20260810_numerical_section/make_figures.py fig6_fields`（其 uniform 行
直接用 tripcolor+parent_cell 标签画迹线，若迹线缺失属同一根因）。

### 10.9 图件现状汇总（最终版，以此节为准）

> 用户已选定 **gptoutput/scripts/ 版**（我的版本）为最终采用，三轮评审修复
> 全部完成。本节为当前权威图件清单；§10.1 描述的是初版，如有出入以本节为准。

#### 两套脚本/输出对照

| 版本 | 脚本位置 | 输出位置 | 状态 |
|---|---|---|---|
| **采用版（我的）** | `deepseekoutput/gptoutput/scripts/{figlib.py, make_final_figures.py}` | `deepseekoutput/gptoutput/figures/`（8 张） | ✅ 三轮评审修复完成，审计通过 |
| 根版（他） | `deepseekoutput/gptoutput/script_gpt/{figlib.py, make_final_figures.py}`（从 `deepseekoutput/` 根复制，只改了接口：FIG_DIR→figures_gpt + METHOD_ORDER 导入） | `deepseekoutput/gptoutput/figures_gpt/`（10 张，含 2 张额外图） | 可独立运行，**未合并、不继续跟进**（其 FEM_TIMING_SCOPE 的 Real-100="solve only" 经查证为误） |

#### 采用版 8 张图当前状态

| 图 | 内容 | 三轮修复要点 | 状态 |
|---|---|---|---|
| fig2_modal_budget_fourspace | r=1,3,9 vs 速度 L2，log-y，三面板同 y 范围 | 注释注明 W0v pending | ✅ |
| fig3_metrics_fourspace | 四指标四面板，log-y | 刻度统一 3 位有效数字百分比 | ✅ |
| fig5_fields_uniform27 | z=0.5 切片：FEM \|u\| + W0n/W1n/W1v 误差，共享色标 | ①布局加宽+色标 p90(W0n)；②双色标加空列（数字不再重叠）；③**全局插值+解析网格迹线**（切片三角化不贴合分区→白边根因） | ✅ |
| fig5_fields_random27 | 同上，random 几何 | ①同左；②同左；③改用归档真实 parent-cell 逐三角形标签 | ✅ |
| fig5_interface_flux_modes | uniform 代表界面 q(s,t)=u·n 四方法 + 误差行 | (s,t) 数值刻度 + 第二行 \|q−q_FEM\| 误差图 | ✅（random 行缺数据） |
| fig7_accuracy_cost_standardized | 每几何一面板：online/首次求解点 + FEM 竖直参考线 | 底部注记 FEM 计时口径差异（三几何其实都是 solve_reference 全程=装配+求解） | ✅（单次计时无散布） |
| fig10_observed_shares_clean | 100% 水平堆叠份额 | 图例两列下移不压 x-label（bbox 验证） | ✅ |
| fig1b_geometry_partition_interfaces | 2×3：固体几何 + 界面线框 | 下半行三几何统一为界面线框（random 提取真实界面三角形边） | ✅ |

#### 图件关键事实（给下一会话）

1. **uniform 切片三角化不贴合网格分区**（364/2280 三角形跨分区平面、无共享边
   连接子域）——因此 uniform 场图必须走 `global_interp=True` + 解析迹线。
   **同样根因可能影响 `20260810_numerical_section/make_figures.py` 的
   fig6_fields uniform 行（其直接 tripcolor，若迹线缺失与此同因），待查。**
2. uniform 场数据已补回主项目 `affine_ddpnm_3d/outputs/benchmark_w1n/
   affine_benchmark_fields.npz`（此前只在 20260810report 归档）。
3. 共享误差色标上界 = **p90(W0n)**（p98 会令 W0n 中位数只占 ~8% 色标而"消失"；
   根版用全量 nanmax 更"诚实"但会复现该问题——用户已确认用 p90 方案）。
4. fig10 的 PNG 在本会话查看器无法渲染（其他图正常），改用 matplotlib bbox
   程序化验证无重叠；下一会话交付前重跑 §10.3 命令即可刷新。
5. 全量布局审计方法：monkeypatch `plt.Figure.savefig` 捕获每张图，检查
   「轴-轴重叠 / 文字落入他轴」——**改任何布局后先跑审计再交付**（见 §10.7）。

#### 图相关待办（按优先级）

1. **W0v 数据**（需用户决策是否重跑 solver，~25min）：补齐后 fig4 + fig2/fig3
   四空间完整版 + fig5 各加 W0v 面板（脚本已自动激活，无需改代码）。
2. **Random-27 接口导出**：把 `20260810report/affine_ddpnm_3d_random_porous/
   run_random_benchmark.py`（~573-612 行）的 interface_q 导出代码 port 回主项目
   并重跑（~12min）→ fig5_interface_flux 补 random 行。
3. **Real-100 场**：修 `z_slice` API 不匹配（一行级）→ 重跑（~4min）→
   fig5_fields_real100。
4. **fig6/fig8/fig9**：需新实验数据（broken-pressure C-DD / ≥3 套网格 /
   多种子），函数已就绪。
5. **根版 script_gpt** 保留作对照，不合并（用户指示）。
6. git：本会话全部产出未提交（gptoutput/ 脚本+图+notes、uniform npz 补回、
   HANDOFF 追加），等用户决定。
