# 交接文档：Affine-DDPNM 驱动示踪剂输运验证（2026-08-11）

> 复制本文件全文到下一个窗口。所有路径基于 `D:\hu\tongjiproj\20260727`。

---

## 0. 本次会话做了什么

1. **新项目 `20260824/affine_ddpnm_tracer/`**：把九模态 Affine-DDPNM 方法应用到示踪剂输运上，几何用随机 27 孔（3D）。自包含项目已跑通，Stokes 指标与 8/10 W1n 归档**逐位一致**。
2. **结合了两个旧项目**：
   - 几何 + DDPNM 方法栈 ← `affine_ddpnm_3d_random_porous`（`random_porous.py`、`affine_face_basis.py`、`ddpnm_core/`、`ddpnm3d/`、`postprocess/` 全量复制）；
   - 示踪剂输运 ← 回收站 `stokes_tracer_hoddpnm`（`tracer_transport.py` 移植：隐式欧拉 + SUPG + 守恒限幅器 + breakthrough/质量平衡指标）。
3. **指标口径统一**：初版用自定义 P1 顶点范数（压力逐孔对齐，与归档不一致）；改为归档同款 `finite_element_error_analysis`（逐孔精确 P2-P1 FE 积分 + 全局均值对齐压力）后，速度 L2 65.320 / 30.749 / 6.506% 与归档 65.32 / 30.75 / 6.51% 完全吻合，压力 21.96 / 6.94 / 4.36% 亦吻合，出口流量误差 0.7272 / 0.1465 / 0.0271 逐位一致。
4. **工作区注意**：`affine_ddpnm_3d_random_porous/affine_face_basis.py` 仍有未提交改动（+VectorConstantFaceBasis，与归档副本内容一致，上次会话遗留）。

## 1. 项目结构

```
20260824/affine_ddpnm_tracer/
├── run_affine_ddpnm_tracer.py   # 主脚本（7 步：网格→FEM→FEM-tracer→三方法 DDPNM+各自 tracer→输出）
├── tracer_transport.py          # 移植的示踪剂求解器 + 限幅器 + 绘图 + CSV
├── random_porous.py             # 随机27球几何/分区（复制）
├── affine_face_basis.py         # Classic/W1n/Affine 界面基（复制）
├── ddpnm_core/  ddpnm3d/  postprocess/   # 方法栈（复制）
└── outputs/benchmark_tracer/    # 全部输出
```

运行（fenicsx 环境，需显式 PATH 导出或用 conda run）：
```powershell
cd D:\hu\tongjiproj\20260727\20260824\affine_ddpnm_tracer
conda run -n fenicsx --no-capture-output python -u run_affine_ddpnm_tracer.py
```
典型耗时 ~3 分钟（FEM 106 s + 三库 ~30 s + tracer 4×0.4 s；首次 JIT 编译另加 ~3 分钟）。

## 2. 核心结果（`outputs/benchmark_tracer/`）

Stokes（速度 rel L2 / 流量误差）：Classic-1 (114 dof) 65.32% / 0.727；
W1n-3 (342 dof) 30.75% / 0.147；**Affine-9 (1026 dof) 6.51% / 0.027**。

Tracer vs FEM 参考（breakthrough L2 / 浓度场 L2 / t90 偏差）：
Classic 6.33e-3 / 1.30e-3 / −0.335；W1n 2.98e-3 / 6.02e-4 / −0.156；
**Affine 5.07e-4 / 1.03e-4 / −0.026**。质量平衡残差全部 ~4e-13（机器精度）。

机制结论：速度误差→示踪剂误差近似线性传递（传递因子 ≈2e-3/单位速度误差）；
界面通量守恒是 Schur 系统的构造性质（每界面 9 矩方程强制归零），流量误差是
受限牵引空间的模型误差而非守恒误差；Affine-9 是唯一把输运误差压到示踪剂
离散误差以下的档位。

## 3. 复现校验点

- 与归档逐位一致的三元组：速度 L2（65.320/30.749/6.506）与流量误差（0.7272/0.1465/0.0271）；
- FEM 出口流量 7.615594e-03（与归档 reference 一致）；
- tracer t90=19.629（FEM），t_final=30 下 breakthrough 充分饱和。

## 4. 已知事项 / 后续

- 速度场注入 tracer 的方式：DDPNM 用 `reconstruct_parent_vertices`（界面顶点双侧平均的 P1 场）；FEM 用 P2→P1 精确插值。这是当前口径，未做灵敏度分析。
- 示踪剂参数（dt=0.1, t=30, 扩散 0.05, SUPG 0.5）为本次选定默认，未扫描。
- 可选后续：tracer 参数扫描 / 均匀 27 球与真实多孔几何的 tracer 对照 / DDPNM 速度场的散度与输运质量误差归因。
